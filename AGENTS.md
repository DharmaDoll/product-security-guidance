# AGENTS.md

## 目的

ここはプロダクトセキュリティエンジニアリングの意思決定を支援する知識基盤である。
文章量、実装数、テスト数を増やすことではなく、読者がセキュリティ上の成果と実装判断を理解できることを優先する。

作業前に`README.md`、`PRINCIPLES.md`、`docs/ARTIFACT_MODEL.md`、
`docs/CONTENT_QUALITY.md`、`docs/SOURCE_POLICY.md`、`docs/ANALYSIS_LENSES.md`を読むこと。

## 成果物の境界

AI領域の作業前には`docs/SECURITY_SCOPE.md`も読むこと。AI Development Securityは開発者端末・IDE・CLI・repository・CIで使う開発agentを対象とする。
アプリケーション自体のAI securityはai-security-foundryへ委ねる。RAG、モデル・データセット、AI application gateway、AI製品のTEVVを移行対象へ戻さない。
汎用agentの資料は開発環境に必要な部分だけを選別する。一般的なApplication Securityは引き続き対象とする。

- `controls/`: 何を満たすべきか。製品固有の手順やテスト用データを正本にしない。
- `engineering/`: 繰り返す設計問題をどう安全に解くか。
- `engineering/**/implementations/`: 製品、プラットフォーム、言語に固有の設定、コード、テスト。
- コントロール配下の`learning.md`: そのコントロールを具体的なシナリオから理解する教材。
- `assessments/`: 組織への導入状況と現在の証拠を判定する方法。
- `mappings/`: 独立して成立した成果物間の関係。
- `sources/`: 参照仕様、一次資料、ガイダンス、利用者提供資料のバージョン、採否、限界。
- `docs/ANALYSIS_LENSES.md`: ポートフォリオの空白と、攻撃段階をまたぐ受け渡し。

同じ本文を複数の成果物へ複製しない。別の成果物に詳細がある場合は、正本へリンクする。

## 執筆ルール

分類は現行の11 domainを維持する。正本は`docs/REPOSITORY_DESIGN.md`、読者の入口は`controls/README.md`。
Controlとpatternは主なdomainに置き、隣接領域はリンク・マッピングで示す。
七つのレイヤー・攻撃段階をdomainの代わりに使わない。基本分類の変更はADRへ記録する。
未移行domainは一覧で明示し、空ディレクトリや本文の複製で埋めない。

1. 最初に読者、問い、守る資産、脅威主体または障害要因を決める。
2. 攻撃が成立する条件、越えられる信頼境界、影響を具体的に書く。
3. セキュリティ不変条件と強制点を、平易な言葉で示す。
4. 一つの具体的なシナリオから始め、固有製品に依存しない原則へ抽象化する。
5. 「危険」「安全」「ベストプラクティス」という言葉だけで理由を済ませない。
6. コントロールへの合格と、システム全体の安全性を同一視しない。
7. 既存の隣接成果物と境界が重なる場合、統合せず違いを説明する。
8. 読者が次の判断をできない長文は、学習資料、パターン、実装例へ分割する。
9. 主なプロダクトセキュリティレイヤー、直接扱う攻撃段階、前後の受け渡しを確認する。
10. 見出し、control title、domain名、pattern名は検索・参照で使う名前を優先する。
    `Dependency release cooldown`や`Untrusted PR boundary`のような重要キーワードを、不自然な日本語へ無理に置き換えない。
11. 専門用語より先に、読者が何を判断・確認するのかを平易な日本語で書く。検索に必要な英語名は括弧で残す。

## AIを使って執筆する場合のルール

- テンプレートの全項目を機械的に埋めない。
- 空の学習資料、実装例、評価を作らない。
- 講義や学習で生じた問い・見方は、まずシナリオ、誤解、判断基準として教材へ残す。一般論を独立した洞察ファイルへ先回りして切り出さない。
- 一つの一般論を複数のコントロールへ展開しない。
- 参照資料にない具体値、製品挙動、対応範囲、証拠を補完しない。
- AIが生成した内容はリポジトリでの解釈であり、参照資料を確認する前に事実として扱わない。
- 大量生成より、少数の成果物を最初から最後まで読み直し、役割の重複を除く。

## 実装と検証

- 主題ごとに[具体化判断](docs/ARTIFACT_MODEL.md#主題ごとの具体化判断)を行い、必要な成果物と完了条件を計画へ残す。技術経路が明確な主題の具体実装を一律に保留せず、全controlへ一律にも要求しない。
- 実装例は、対象と変更点に加え、手元または使い捨ての対象へ入れる最短手順、安全なsmoke test、解除方法を示す。配置しただけで完成とせず、主張する成功・拒否・失敗の代表経路を観測可能にする。
- 実装で分かった制約、迂回経路、失敗時の挙動を、patternの選択条件とcontrolの境界へ戻して見直す。
- コントロール記録へテストコードを置かない。製品非依存の「診断で確認する項目」は記載できる。
- 「診断で確認する項目（異常時テスト）」は、脆弱性診断や設計レビューで使うチェックリストだけでも成果物として認める。実装例やテストコードの作成・実行は必須ではない。記載方法は[文書品質](docs/CONTENT_QUALITY.md#failure-checks)に従う。
- テストコードは、対象となる実装例が存在し、観測可能なセキュリティ特性がある場合だけ追加する。
- 危険な実装例は、比較による学習価値がある場合に限り、隔離して作る。
- SaaS設定や組織運用を、架空のテスト用データで`PASS`にしない。具体的なガイダンスまたは評価に記載する。
- 失敗、タイムアウト、入力不足、部分的な収集を「問題なし」という結果に変換しない。
- 秘密情報、非公開の識別子、本番データをテスト、ログ、証拠へ入れない。

## 参照資料とマッピング

- 一次資料を優先し、バージョン、改訂、確認日を記録する。
- 規範資料、調査資料、リポジトリでの解釈、学習過程で導出した問い・見方を区別する。
- 参照資料ごとに、採用した判断、変更して採用した判断、不採用とした提案、限界を追跡する。
- コントロール本文から詳細を分離しても、参照仕様を省略しない。参照資料記録を残し、セキュリティ特性または
  実装例から参照資料IDをたどれるようにする。
- `REF-PORTFOLIO-001`はポートフォリオの七つのレイヤー、`LOCAL-SUPPLY-CHAIN-ATTACK-STAGES`は十二の攻撃段階と
  受け渡しの分析に使う。どちらも個別要件、準拠、導入済み状態へ自動変換しない。
- 参照資料IDは資料の主な役割に合わせる。移行時に役割と命名を再評価し、旧IDを残す場合は移行台帳だけに記録する。
- セキュリティ特性の直接の根拠と、横断分析にだけ使う資料を同じ`source_refs`へ混ぜない。
- マッピングは別の成果物として作り、コントロールやパターンの意味をマッピングから逆算しない。
- 準拠、完全な対応範囲、組織への導入済み状態を、根拠なく主張しない。

## 完了条件

新しい成果物は、その種類に応じて次を満たす。

- コントロール: 問い、直接の失敗、適用範囲、セキュリティ特性、境界が明確。
- 学習資料: シナリオ、用語、悪用経路、誤解、判断基準を単独で理解できる。
- パターン: アーキテクチャ、選択肢、コントロールの配置、トレードオフ、失敗経路が実装判断に使える。
- 実装例: 対象バージョン、変更箇所、前提、最短導入手順、安全なsmoke test、解除方法、制限が明確。
- マッピング: 関係する両方の成果物が独立して存在し、関係と根拠を説明できる。
- 横断分析: 分析軸の出典、直接対応、隣接領域、受け渡し、空白、限界を区別できる。
- 参照資料: 役割、発行者、参照したバージョンまたは確認日、利用箇所、採否、限界を説明できる。

数合わせのファイルがないこと、ローカルリンクが解決すること、旧成果物との関係が
`docs/MIGRATION.md`へ記録されていることも確認する。
